# 媒介助手依赖(纯跨平台)。安装:双击「安装依赖」脚本,或手动 pip install -r requirements.txt
flask>=3.0        # 本地网页服务(里程碑 C)
requests>=2.28    # relay 与飞书 HTTP 调用
pillow>=9.0       # 生成 relay 视觉自检测试图
openpyxl>=3.1     # 读/写复核 Excel
pyyaml>=6.0       # 解析 config.yaml
