# 录录媒 · 使用说明

这是一个在你自己电脑上运行的小工具。它帮你把微信里收到的媒体老师文字和截图,自动整理成表格里的字段(姓名、电话、身份证、收款方式、去程/返程行程),你核对无误后一键回填到飞书表格。

全程在网页里点鼠标完成,**不需要打开命令行,也不需要手动改 Excel**。

---

## 第一次使用(只做一次)

1. **确认电脑有 Python**
   没装过的话,去 https://www.python.org/downloads/ 下载安装。
   安装时请务必勾选 **“Add python.exe to PATH”**(Mac 版无此选项,直接装即可)。

2. **安装依赖**
   - Windows:双击 `安装依赖.bat`
   - Mac:双击 `安装依赖.command`(若提示无法打开,右键 → 打开)

   窗口里出现 “完成” 字样即可关闭。这一步联网下载几个组件,只需做一次。

3. **填一次配置**
   把 `config.example.yaml` 复制一份,改名为 `config.yaml`,用记事本打开填好:
   - relay 的 `api_key`(识别截图要用)
   - 飞书的 `app_id` / `app_secret`(回填要用)
   - `target_url` 目标表链接(回填写入的那张表)

   > `config.yaml` 里有密钥,请勿外发、勿上传公开仓库。

---

## 每天怎么用

1. **启动**
   - Windows:双击 `启动媒介助手.bat`
   - Mac:双击 `启动媒介助手.command`

   会弹出一个黑色窗口,并自动打开浏览器到 `http://127.0.0.1:8765/`。
   **这个黑窗口不要关**——它就是工具本体,关掉工具就停了。用完再关。

2. **网页里三步走**
   页面顶部是「录录媒」标识,下面三个按钮就是三步,点按钮切换:

   - **① 选活动**:新建一个活动(比如“XX发布会”),或从下面的「活动历史」表里点一行,进入之前建好的活动继续录入。
   - **② 资料识别**:先把这场活动收到的文字和截图拖进去上传(一人一个单元识别更准),再点“开始识别”,等进度条走完。下面会按人出现核对卡片:
     - **红色格** = 格式有错(比如电话不是 11 位),鼠标停上去看原因,直接改。
     - **黄色格** = AI 不太确定,请你确认。
     - “待认领行程”里的行程没对上人,请在下拉里选出行人。
     改完点“保存修改”,红黄标记会实时刷新。
   - **③ 回填飞书**:点“试算预览”,核对每一列落点没问题,再点“确认写入飞书”,数据就追加到目标表。

   > 右上角的齿轮 ⚙ 是「设置」:确认 relay、飞书都填好了,点“检查识别引擎”“检查飞书连接”,都变绿表示通。首次用一次即可。

3. **用完关掉那个黑窗口**,工具即停止。下次要用再双击启动。

---

## 常见问题

- **浏览器没自动打开?** 手动在浏览器地址栏输入 `http://127.0.0.1:8765/`。
- **窗口一闪就没了 / 提示缺少模块?** 先双击一次“安装依赖”,再启动。
- **提示端口被占用?** 窗口里会打印实际使用的网址,照着打开即可。
- **想换目标表?** 改 `config.yaml` 里的 `target_url`,重启工具。

> 数据只在你本机和公司内网(relay、飞书租户)之间流转,不经过任何第三方。
